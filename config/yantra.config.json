{
  "description": "Yantra configuration - Session enhancement settings",

  "skills": {
    "context_analysis": {
      "name": "Context Analysis",
      "description": "Analyze current project context (git, files, system state)",
      "enabled": true
    },
    "safety_gates": {
      "name": "Safety Gates",
      "description": "Prevent destructive operations (rm -rf, git reset, etc.)",
      "enabled": true
    }
  },

  "headroom": {
    "enabled": true,
    "args": ["--no-mcp"]
  },

  "environment": {
    "YANTRA_VERSION": "0.1.0"
  }
}
